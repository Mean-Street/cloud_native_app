- put jenkins job in github.yml (use config.xml file to do so)
- cf. ansible command to create job (without restarting jenkins)
- jenkinsfile gives $BRANCH_NAME to a script that handles branches differently

