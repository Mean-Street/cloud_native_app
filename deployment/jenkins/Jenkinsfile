pipeline {
	agent any
	stages {
		stage('Test') {
			when {branch 'dev'}
			steps {
				sh 'printenv'
				sh './deployment/jenkins/scripts/test.py'}
		}
		stage('Deploy') {
			when {branch 'master'}
			steps {sh './deployment/jenkins/scripts/deploy.py'}
		}
	}
}
